# /plan-module 命令

单独执行模块规划阶段，生成规格文档但不实现代码。

## 输入

用户提供模块的简短描述：$ARGUMENTS

## 执行

派生 planner Agent：

> 请为以下游戏核心机制生成完整的模块规格文档：「$ARGUMENTS」

完成后向用户展示规格概要，等待反馈。

如需继续实现，用户可以运行 `/build-module` 并指定已有的规格文档。
